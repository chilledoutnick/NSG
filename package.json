{
  "name": "nsg-crm-root",
  "version": "1.0.0",
  "scripts": {
    "heroku-postbuild": "cd frontend && npm install --legacy-peer-deps --include=dev && CI=false npm run build"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
